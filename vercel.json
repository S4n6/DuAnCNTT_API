{
  "version": 2,
  "builds": [
    {
      "src": "/package.json",
      "use": "@vercel/node",
      "config": {
        "distDir": "dist/apps/user"
      }
    },
    {
      "src": "/package.json",
      "use": "@vercel/node",
      "config": {
        "distDir": "dist/apps/participants"
      }
    }
  ],
  "routes": [
    {
      "src": "/user/(.*)",
      "dest": "dist/apps/user/$1"
    },
    {
      "src": "/participants/(.*)",
      "dest": "dist/apps/participants/$1"
    }
  ]
}